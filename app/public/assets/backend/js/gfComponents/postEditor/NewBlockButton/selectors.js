export const selectors = Object.freeze({
   classes: {
       addNewButton: 'addNewBlock',
       buttonContainer: 'buttonContainer',
       blockListContainer: 'blockListContainer',
       show: 'show',
       active: 'active'
   }
});